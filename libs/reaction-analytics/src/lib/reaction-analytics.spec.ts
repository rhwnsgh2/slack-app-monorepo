import { reactionAnalytics } from './reaction-analytics';

describe('reactionAnalytics', () => {
    it('should work', () => {
        expect(reactionAnalytics()).toEqual('reaction-analytics');
    })
})